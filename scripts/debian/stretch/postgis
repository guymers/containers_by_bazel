postgresql-${POSTGRESQL_MINOR_VERSION}=${POSTGRESQL_DEB_VERSION}
postgis=${POSTGIS_DEB_VERSION}
postgresql-${POSTGRESQL_MINOR_VERSION}-postgis-${POSTGIS_MINOR_VERSION}=${POSTGIS_POSTGRESQL_DEB_VERSION}
postgresql-${POSTGRESQL_MINOR_VERSION}-postgis-${POSTGIS_MINOR_VERSION}-scripts=${POSTGIS_POSTGRESQL_DEB_VERSION}
